# 视频分享分类上线验证记录

日期：2026-08-29  
生产项目：`eqdfyhqeqkbjvivscjau`（EM-BOX）  
分支：`ylif4-fix`

## 数据保护

- 迁移前正向/负向有效课程：37 条（正向 24、负向 13）。
- 迁移前课程内容指纹：`3311951e69fc2dc821dc09b451a548b2`。
- 迁移后正向/负向有效课程：37 条。
- 迁移后课程内容指纹：`3311951e69fc2dc821dc09b451a548b2`。
- 迁移后 37 条课程的新分类字段非空数量：0。
- 迁移后课程质量标签关联数量：0。

迁移只新增分类表、关联表、可空字段、约束、索引及默认字典，没有更新或删除现有课程。

## 默认字典

- 任务分类：5 项。
- 正向质量标签：7 项。
- 负向问题标签：10 项。

后台可新增、改名、排序和停用；已被课程使用的项目不能删除。

## 部署与验证

- Supabase `embox-api`：已部署，保留自定义认证模式（`verify_jwt=false`）。
- Supabase `training-public`：已部署，保持公开免登录（`verify_jwt=false`）。
- 生产公开函数无 Authorization 请求：HTTP 200，返回 38 条资料、38 个分享令牌。
- 聚焦 Vitest：4 个测试文件、19 个测试通过。
- 前端生产构建：通过。
- Express 后端 TypeScript：通过。
- 两个 Edge Function 打包解析：通过。
- 手机视口 Playwright：通过；正负向切换、任务分类、质量标签、严重度和页面宽度均验证。

## 兼容策略

- 旧课程的 `video_polarity` 为空时，继续根据原 `category` 推导正向或负向。
- 旧课程的 ID、视频 URL、章节、资料、动作流和分享令牌不变。
- 新建或主动编辑视频时才保存新分类，并继续同步旧 `category` 字段。
